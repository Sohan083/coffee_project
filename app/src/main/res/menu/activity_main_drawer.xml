<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/outlet_registration_nav_graph"
            android:icon="@drawable/ic_retail"
            android:title="@string/menu_pos_registration" />
        <item
            android:id="@+id/outlet_activity_nav_graph"
            android:icon="@drawable/ic_retail"
            android:title="@string/menu_outlet_activity" />
        <item
            android:id="@+id/attendance_nav_graph"
            android:icon="@drawable/ic_calendar"
            android:title="@string/menu_attendance" />

        <item
            android:id="@+id/logout_nav_graph"
            android:icon="@drawable/ic_logout"
            android:title="@string/menu_logout" />
    </group>

</menu>
